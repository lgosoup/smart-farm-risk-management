# GPU / LLM inference (optional — install only when running the local LLM)
# pip install -r requirements-gpu.txt --extra-index-url https://download.pytorch.org/whl/cu118
torch==2.5.1+cu118
torchaudio==2.5.1+cu118
torchvision==0.20.1+cu118
transformers>=4.36.0
bitsandbytes>=0.41.0
